<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.testproject.domain.notice.NoticeDao">

	<insert id="insert">
		INSERT INTO notice(notice_title, notice_coname)
        VALUES(#{noticeTitle}, #{noticeConame})
	</insert>

	<select id="findAll"
		resultType="site.metacoding.testproject.domain.notice.Notice">
	</select>

	<select id="findById"
		resultType="site.metacoding.testproject.domain.notice.Notice">

	</select>

	<update id="update">

	</update>

	<delete id="deleteById">

	</delete>


</mapper>