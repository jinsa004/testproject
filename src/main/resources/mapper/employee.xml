<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.testproject.domain.employee.EmployeeDao">

	<insert id="insert">
		INSERT INTO employee(employeeName, employeeBirth, employeeSex, employeeUsername, employeePassword, employeeEmail, employeeTel, employeeLocation)
		VALUES(#{employeeName}, #{employeeBirth}, #{employeeSex}, #{employeeUsername}, #{employeePassword}, #{employeeEmail}, #{employeeTel}, #{employeeLocation});
	</insert>

	<select id="findAll"
		resultType="site.metacoding.testproject.domain.employee.Employee">
	</select>

	<select id="findById"
		resultType="site.metacoding.testproject.domain.employee.Employee">

	</select>

	<update id="update">

	</update>

	<delete id="deleteById">

	</delete>


</mapper>